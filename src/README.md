# src/

This folder is reserved for reusable Python modules (e.g. `preprocessing.py`,
`train.py`, `evaluate.py`) if the notebook logic gets refactored into
standalone scripts later. For now, the full pipeline lives in
`notebooks/NSL_KDD_Intrusion_Detection.ipynb`.
